<h1> visualiznado regitro {{ id }}</h1>
<p> nome: {{nome}}</p>

<p> Email: {{ email }} </p>
<form action="/{{ id }}" method="POST">
    <input type="text" name="email">
    <button>Salvar</button>
</form>
